<form className="">
                  <div className="flex items-center -mx-[6px]">
                    <div className="basis-1/3 px-[6px]">
                      <Popover open={open} onOpenChange={setOpen}>
                        <PopoverTrigger asChild>
                          <Button
                            variant="outline"
                            role="combobox"
                            aria-expanded={open}
                            className="w-full  rounded-none border border-solid border-[#d9d9d9] bg-[#fff] pt-[21px] pb-[6px] pr-[40px] pl-[10px] relative text-[14px] text-left justify-start"
                          >
                            <div className="absolute right-0 center-y w-[40px] h-full  flex items-center justify-center">
                              <span className="w-0 h-0 border-l-[6px] border-l-transparent border-r-[6px] border-r-transparent border-t-[8px] border-t-[#828282] "></span>
                              <div className="absolute left-0 w-[0.3px] h-[20px] bg-vendor center-y"></div>
                            </div>
                            <label
                              htmlFor="province"
                              className="mt-[4px] px-[10px] text-[10px] text-vendor absolute top-0 right-0 left-0 text-left"
                            >
                              Tỉnh /thành
                            </label>
                            {provinceValue ? provinceValue : "Chọn tỉnh/thành"}
                          </Button>
                        </PopoverTrigger>
                        <PopoverContent className="w-full p-0 rounded-none">
                          <Command>
                            <CommandInput placeholder="Tìm kiếm tỉnh/thành..." />
                            <CommandList>
                              <CommandEmpty>Không có dữ liệu.</CommandEmpty>
                              <CommandGroup>
                                {getProvinceQuery.data.data.data.map(
                                  (province) => (
                                    <CommandItem
                                      key={province.id}
                                      value={province}
                                      onSelect={(currentValue) => {
                                        setProvinceValue(currentValue);
                                        setDistrictListValue(null);
                                        setDistrictList(null);
                                        setWardListValue(null);
                                        setWardList(null);
                                        setOpen(false);
                                      }}
                                    >
                                      <Check
                                        className={cn(
                                          "mr-2 h-4 w-4",
                                          provinceValue === province.full_name
                                            ? "opacity-100"
                                            : "opacity-0"
                                        )}
                                      />
                                      {province.full_name}
                                      {/* current value */}
                                    </CommandItem>
                                  )
                                )}
                              </CommandGroup>
                            </CommandList>
                          </Command>
                        </PopoverContent>
                      </Popover>
                    </div>
                    <div
                      className={`basis-1/3 px-[6px]  ${
                        !district && !districtValue
                          ? "opacity-50 cursor-not-allowed"
                          : ""
                      }`}
                    >
                      <Popover
                        open={openDistrict}
                        onOpenChange={setOpenDistrict}
                      >
                        <PopoverTrigger asChild>
                          <Button
                            variant="outline"
                            role="combobox"
                            aria-expanded={openDistrict}
                            className={`w-full  rounded-none border border-solid border-[#d9d9d9] bg-[#fff] pt-[21px] pb-[6px] pr-[40px] pl-[10px] relative text-[14px] text-left justify-start ${
                              !district && !districtValue
                                ? "opacity-50 cursor-not-allowed"
                                : ""
                            }`}
                          >
                            <div className="absolute right-0 center-y w-[40px] h-full  flex items-center justify-center">
                              <span className="w-0 h-0 border-l-[6px] border-l-transparent border-r-[6px] border-r-transparent border-t-[8px] border-t-[#828282] "></span>
                              <div className="absolute left-0 w-[0.3px] h-[20px] bg-vendor center-y"></div>
                            </div>
                            <label
                              htmlFor="province"
                              className="mt-[4px] px-[10px] text-[10px] text-vendor absolute top-0 right-0 left-0 text-left"
                            >
                              Quận / huyện
                            </label>
                            {districtValue ? districtValue : "Chọn quận huyện"}
                          </Button>
                        </PopoverTrigger>
                        <PopoverContent className="w-full p-0 rounded-none">
                          <Command>
                            <CommandInput placeholder="Tìm kiếm tỉnh/thành..." />
                            <CommandList>
                              <CommandEmpty>Không có dữ liệu.</CommandEmpty>
                              <CommandGroup>
                                {district?.map((district) => (
                                  <CommandItem
                                    key={district.id}
                                    value={district}
                                    onSelect={(currentValue) => {
                                      setDistrictListValue(currentValue);
                                      setWardListValue(null);
                                      setWardList(null);
                                      setOpenDistrict(false);
                                    }}
                                  >
                                    <Check
                                      className={cn(
                                        "mr-2 h-4 w-4",
                                        districtValue === district.full_name
                                          ? "opacity-100"
                                          : "opacity-0"
                                      )}
                                    />
                                    {district.full_name}
                                  </CommandItem>
                                ))}
                              </CommandGroup>
                            </CommandList>
                          </Command>
                        </PopoverContent>
                      </Popover>
                    </div>
                    <div
                      className={`basis-1/3 px-[6px]  ${
                        !ward ? "opacity-50 cursor-not-allowed" : ""
                      }`}
                    >
                      <Popover open={openWard} onOpenChange={setOpenWard}>
                        <PopoverTrigger asChild>
                          <Button
                            variant="outline"
                            role="combobox"
                            aria-expanded={openWard}
                            className={`w-full  rounded-none border border-solid border-[#d9d9d9] bg-[#fff] pt-[21px] pb-[6px] pr-[40px] pl-[10px] relative text-[14px] text-left justify-start ${
                              !ward ? "opacity-50 cursor-not-allowed" : ""
                            }`}
                          >
                            <div className="absolute right-0 center-y w-[40px] h-full  flex items-center justify-center">
                              <span className="w-0 h-0 border-l-[6px] border-l-transparent border-r-[6px] border-r-transparent border-t-[8px] border-t-[#828282] "></span>
                              <div className="absolute left-0 w-[0.3px] h-[20px] bg-vendor center-y"></div>
                            </div>
                            <label
                              htmlFor="province"
                              className="mt-[4px] px-[10px] text-[10px] text-vendor absolute top-0 right-0 left-0 text-left"
                            >
                              Phường / xã
                            </label>
                            {wardValue ? wardValue : "Chọn phường/xã"}
                          </Button>
                        </PopoverTrigger>
                        <PopoverContent className="w-full p-0 rounded-none">
                          <Command>
                            <CommandInput placeholder="Tìm kiếm phường/xã..." />
                            <CommandList>
                              <CommandEmpty>Không có dữ liệu.</CommandEmpty>
                              <CommandGroup>
                                {ward?.map((w) => (
                                  <CommandItem
                                    key={w.id}
                                    value={w}
                                    onSelect={(currentValue) => {
                                      setWardListValue(currentValue);
                                      setOpenWard(false);
                                    }}
                                  >
                                    <Check
                                      className={cn(
                                        "mr-2 h-4 w-4",
                                        wardValue === w.full_name
                                          ? "opacity-100"
                                          : "opacity-0"
                                      )}
                                    />
                                    {w.full_name}
                                  </CommandItem>
                                ))}
                              </CommandGroup>
                            </CommandList>
                          </Command>
                        </PopoverContent>
                      </Popover>
                    </div>
                  </div>
                </form>